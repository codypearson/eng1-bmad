# Team / enterprise overrides for _bmad/config.toml.
# Committed to the repo — applies to every developer on the project.
# Tables deep-merge over base config; keyed entries merge by key.
#
# Layout: this repo is the BMAD control plane for multiple application
# projects. BMAD artifacts stay here; application code lives elsewhere.

[core]
# Pin artifact root to this BMAD repo (survives reinstall answer drift).
output_folder = "{project-root}/_bmad-output"

[modules.bmm]
planning_artifacts = "{project-root}/_bmad-output/planning-artifacts"
implementation_artifacts = "{project-root}/_bmad-output/implementation-artifacts"
project_knowledge = "{project-root}/docs"
